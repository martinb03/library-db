<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d" d:DesignWidth="1024" d:DesignHeight="600"
        Width="1024"
        Height="600"
        x:Class="LIbraryUI.MainWindow"
        Title="LibraryUI">
    <Grid Background="{DynamicResource PrimaryBackground}" ColumnDefinitions="200, *">
       
            <Border Padding="15" Background="{DynamicResource PrimaryBackgroundGradient}">
                <Grid RowDefinitions="*,Auto">
                    <StackPanel Spacing="15">
                        <Button Content="Books" HorizontalAlignment="Stretch"></Button>
                        <Button Content="Borrowings" HorizontalAlignment="Stretch"></Button>
                        <Button Content="Penalties" HorizontalAlignment="Stretch"></Button>
                        <Button Content="Customers" HorizontalAlignment="Stretch"></Button>
                        <Button Content="Books" HorizontalAlignment="Stretch"></Button>
                        <Button Content="Books" HorizontalAlignment="Stretch"></Button>
                    </StackPanel>
                </Grid>
            </Border>
            
        </Grid>    
</Window>
